
<p><%= link_to "back to home", lists_path %></p>

<p><%= link_to "add a movie", new_list_bookmark_path(@list)%>

<p><strong> Add new movie </strong></p>
<ul class="list-group">
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item"><%= bookmark.comment %><%= link_to "delete movie", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></li>
  <% end %>
</ul>
<br>
